# окружение
.venv/
venv/
.env
*.pyc
__pycache__/

# Django
db.sqlite3
db.sqlite3-journal
/media/
/staticfiles/
*.log

# IDE
.idea/
.vscode/
.DS_Store
.ruff_cache/

# статический экспорт демо
dist/

# бэкапы с сервера (docker compose, сервис backup)
backups/
.last-deployed

# рабочие таблицы организаторов: данные из них попадают в репозиторий
# разобранными (apps/content/lecture_catalog.py), сами файлы — нет
*.xlsx
*.xls
# шаблоны документов (согласия и т. п.) — их текст переносится в код
*.docx
*.doc
# документы, которые кладут в корень проекта для работы (согласия и т. п.)
/*.pdf
# файлы блокировки офисных редакторов, пока документ открыт
.~lock.*#
~$*
